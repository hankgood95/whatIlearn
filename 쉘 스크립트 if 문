쉘 스크립트에서도 if 문이 있다.
if로 시작하고 그 안에 명령어가 끝나면 fi로 닫아줘야 한다.

사용 방법은 아래와 같다.
if[[$변수 == "Y"]];then
  명령어
fi

여기서 이제 조건 연산자에 대해서 알아보자.
== 는 뭐 당연히 알것이다. 그러니 패스

-lt : 보다 작을때
if[[ $date -lt $nowdate ]]
이건 날짜값이 현재 날짜보다 작을때를 의미한다.

-ge : ~보다 클때
사용법은 위와 같으니 패스
